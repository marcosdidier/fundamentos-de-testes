[project]
name = "llm-metamorphic-testing"
version = "0.1.0"
description = "Avaliacao de testes metamorficos em LLMs para classificacao de intencoes."
requires-python = ">=3.11"
dependencies = []

[project.optional-dependencies]
dev = [
    "pytest>=8.0",
]

[tool.pytest.ini_options]
pythonpath = ["src"]
testpaths = ["tests"]

